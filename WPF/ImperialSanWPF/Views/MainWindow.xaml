<Window x:Class="ImperialSanWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImperialSanWPF"
        mc:Ignorable="d"
        Title="Imperial San" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка -->
        <StackPanel Grid.Row="0" Background="#222225" Height="64" Orientation="Horizontal" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Image Source="/Resources/Images/logo.png" Height="35" Width="35" HorizontalAlignment="Left" Margin="15,15,5,15"/>
                <TextBlock Text="Imperial San" FontSize="18" Foreground="#EC7133" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Center">
                <Button x:Name="loginButton" Content="Войти" Click="loginButton_Click"/>
            </StackPanel>
        </StackPanel>

        <Frame x:Name="mainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
